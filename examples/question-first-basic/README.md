# Question-First Basic

A minimal question-first Surface Shell.

Commands:

```text
/config
/corpus
/trace
/help
```

Plain input goes to the question handler. See `shell.ts` and `transcript.golden.md`.
