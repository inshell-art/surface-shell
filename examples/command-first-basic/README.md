# Command-First Basic

A minimal command-first Surface Shell.

Commands:

```text
config
config local
config direct
prompt
run
current
help
```

See `shell.ts` for the shell definition and `transcript.golden.md` for expected behavior.
