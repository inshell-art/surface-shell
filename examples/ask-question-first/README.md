# Ask Question-First Example

This example shows how a source-grounded app can keep normal input as questions while exposing state-aware slash commands.

Run `/config` or `/corpus` to see branch help. Plain text such as `What is PATH?` is parsed as a question handoff.
