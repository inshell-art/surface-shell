# THOUGHT CLI Reference

This reference documents the adopted command-first shape without importing THOUGHT application code.

```text
command-first
prompt: thought>
branch commands: config, prompt, run, mint, path, wallet
side-effect gates: wallet, contract, external-model
```

See `command-tree.md`, `transcript.example.md`, and `adoption-notes.md`.
