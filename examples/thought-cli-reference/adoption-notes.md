# THOUGHT-Like Adoption Notes

- Keep model routing, prompt loading, wallet state, authorization, minting, and provenance outside Surface Shell Core.
- Use branch help for `config`, `config direct`, `mint`, and `path`.
- Keep secret redaction rules near app-owned key commands.
- Use side-effect gates for wallet, contract, and external-model boundaries.
- Preserve legacy aliases as hidden nodes or aliases while teaching canonical commands.
