# THOUGHT Command-First Example

This example shows how a side-effectful command-first app can expose branch commands without moving model, wallet, PATH, authorization, mint, or provenance logic into Surface Shell core.

Run `config`, `config direct`, or `mint` to see state-aware branch returns. Run `config direct key sk-test` to see secret redaction policy.
