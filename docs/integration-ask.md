# Ask Integration Notes

Ask should keep question handling, corpus retrieval, evidence packs, model planning, live resources, hard boundary guards, source trace, and answer generation outside this package.

The core should only help Ask expose question-first slash commands and state-aware branch returns.
