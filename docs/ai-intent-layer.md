# AI Intent Layer

The v0.1 core does not implement AI intent parsing.

Future app integrations may use Surface Shell metadata to suggest commands from natural language. Meaningful side effects should still require explicit command boundaries.
