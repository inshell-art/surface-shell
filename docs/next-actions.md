# Next Actions

Next actions are suggested commands attached to a return.

They are not autonomous execution. They tell the user what command can safely move the shell forward.

```text
run unavailable

state
  prompt  missing
  route   missing

next
  prompt <text>
  config
```

Use next actions for missing requirements, repair paths, and preferred branch navigation.
