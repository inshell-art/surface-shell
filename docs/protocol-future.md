# Protocol Future

Surface Shell is currently an interface paradigm and a TypeScript reference implementation.

It is not yet a wire protocol.

A future Surface Shell Protocol may define stable JSON schemas for:

- command trees
- command nodes
- surface returns
- state lines
- next actions
- side-effect gates
- events / traces

Do not claim protocol stability until multiple independent runtimes interoperate through those schemas.
